# 튜플
tp = (10, 20, 30)

# 인덱싱과 슬라이싱은 리스트와 동일
print(f'tp[0] = {tp[0]}')
print(f'tp[:2] = {tp[:2]}')

# + 와 * 연산은 리스트와 동일
tp = tp * 3
print(f'tp * 3 = {tp}')

# 각 요소에 변수 할당 가능
tpx, tpy = (10, 20)
print(f'tpx = {tpx}, tpy = {tpy}')

# 요소 추가, 수정, 삭제 불가능 - 튜플의 특징
# 한번 결정된 요소는 변경 불가능