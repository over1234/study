#include <stdio.h>

long long int n;



long long int abs(long long int k)
{
	
	if (k < 0)
	{
		k = (k * -1);
		return k;
	}
	else if (k > 0)
	{
		return k;
	}
}
int main()
{
  scanf("%lld", &n);
  printf("%lld\n", abs(n));
  return 0;
}