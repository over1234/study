#include <stdio.h>

int n;



void f(int k)
{
	int sum = 0;
	for (int i = 1; i <= n; i++)
	{
		if (k % i == 0)
		{
			sum = sum + i;

		}
	}

	if (sum == k + 1)
	{
		printf("prime");
	}
	else
	{
		printf("composite");
	}
}
int main()
{
  scanf("%d", &n);
  f(n);
  return 0;
}